<div class="row">
  <div class="large-12 columns">
    <h2>Sign up</h2>

    <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>
  </div>

  <div class="large-12 columns"><%= f.label :name %><br />
  <%= f.text_field :name, :autofocus => true %></div>

  <div class="large-12 columns"><%= f.label :email %><br />
  <%= f.email_field :email %></div>

  <div class="large-12 columns"><%= f.label :password %><br />
  <%= f.password_field :password %></div>

  <div class="large-12 columns"><%= f.label :password_confirmation %><br />
  <%= f.password_field :password_confirmation %></div>

  <div class="large-12 columns"><%= f.submit "Sign up", class: "button radius round" %></div>
<% end %>

  <div class="large-12 columns"><%= render "devise/shared/links" %></div>
</div>
